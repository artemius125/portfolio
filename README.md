# Портфолио: LLM-сервисы и бэкенд на Python

Инженер-бэкендер с фокусом на прикладных LLM-системах. Делаю сервисы, где модель — часть
продукта, а не демо: с детерминированным ядром, защитой от галлюцинаций, ограничением
расходов, панелью управления для не-инженеров и внятным поведением при отказах внешних API.

Ниже — шесть рабочих проектов, разобранных как кейс-стади. Заказчик и его домены обезличены,
исходный код не публикуется: это разбор задач, архитектуры и инженерных решений, написанный
по факту сделанного. Метрики приводятся только там, где их подтверждают код, конфигурация
или тесты; там, где чисел нет, честно сказано, что их нет.

## Стек

**Языки и рантайм:** Python 3.10+ (async/await), в контейнерах 3.12; немного JS для
встраиваемых виджетов.

**Веб:** FastAPI, Starlette, SSE-стриминг, Flask + HTMX там, где не нужен SPA;
server-rendered Jinja2-админки вместо фронтенд-сборки.

**Данные:** PostgreSQL, SQLAlchemy 2.x (async, `asyncpg` / `psycopg` 3), Alembic-миграции,
`JSONB`, частичные индексы, upsert-идемпотентность, advisory-локи, ретеншн-политики.

**LLM:** OpenAI-совместимые API (шлюз-агрегатор моделей), модели класса `gpt-4.1-mini`,
`qwen3.7-plus`, `deepseek-v4-*`, эмбеддинги `text-embedding-3-large`. Function calling,
JSON-режим, стриминг, отдельные агентные циклы с ограничением шагов, LLM-as-judge,
пакетная офлайн-разметка под бюджетом. Учёт токенов и стоимости как продуктовая функция.

**Поиск и ранжирование:** field-weighted BM25 с IDF и нормализацией длины, лёгкий стемминг
для русского, косинусный поиск по эмбеддингам на numpy, каскадные скоринговые пайплайны с
объяснимым разложением риска.

**Инфраструктура:** Docker, Kubernetes (реплики, RollingUpdate, readiness/liveness/startup-пробы,
requests/limits, nodeSelector и tolerations), Helm values, GitLab CI (шаблонный Auto DevOps
и собственные джобы с `helm upgrade --install`), Loki + Grafana для логов, Sentry.

**Инженерная гигиена:** pytest с фейковыми LLM-клиентами (сеть в тестах не нужна),
golden-тесты и eval-харнессы с ассертами на метрики, fail-closed конфигурация, санитайзинг
секретов в логах, обезличивание персональных данных в датасетах.

## Проекты

| № | Кейс | Суть в одну строку |
|---|---|---|
| 01 | [ИИ-консультант по рекламным услугам](cases/01-ai-sales-consultant.md) | Встраиваемый чат-виджет консультирует по каталогу рекламных услуг, передаёт заявку менеджеру и полностью управляется отделом продаж через админку — прайс, промпт с версиями, квоты, аналитика воронки. |
| 02 | [Пайплайн автоматических дайджестов](cases/02-content-digest-pipeline.md) | Ежедневный и еженедельный дайджест: сбор материалов из трёх источников, генерация через LLM в строгом JSON, экспорт готового для CMS HTML, публикация файла в облако по расписанию, живой лог и учёт токенов в панели редактора. |
| 03 | [Модерация комментариев](cases/03-comment-moderation.md) | Каскад из шести слоёв даёт объяснимый балл риска «это скрытая реклама», LLM работает арбитром только в серой зоне, результат — очередь ревью для модератора без автоудалений. |
| 04 | [ИИ-подбор учебных треков](cases/04-learning-path-advisor.md) | Детерминированный BM25-движок собирает учебный трек из каталога, LLM ведёт диалог и решает, когда его вызвать; три независимых рубежа не дают модели выдумать курс, цену или ссылку. |
| 05 | [Бот поддержки для открытых линий Битрикс24](cases/05-support-bot-bitrix24.md) | База знаний построена из истории реальных переписок операторов (с обезличиванием и разметкой под бюджетом), бот отвечает текстом статьи дословно, а маршрутизацией и эскалацией управляют детерминированные guard'ы поверх LLM. |
| 06 | [Централизованные логи](cases/06-observability-stack.md) | Loki + Grafana в неймспейсе без прав уровня кластера: логи пушат сами приложения через неблокирующую очередь, дашборд и деплой стека — как код в CI. |

## Как читать эти кейсы

Каждый документ построен одинаково: задача и контекст с реальными ограничениями →
архитектурная диаграмма → стек по фактам → инженерные решения с альтернативами и платой за
выбор → что было сложно (с настоящими багами и инцидентами, а не абстракциями) →
результат → моя роль.

Самое интересное — четвёртый и пятый разделы. Там видно, как выглядят настоящие компромиссы:
почему in-memory rate-limit вместо Redis, почему policy-lock сильнее вердикта модели, почему
дедлок на блокирующем `readline()` проявился как «два неуспешных запуска подряд», и почему
калибровка порога иногда доказывает предел подхода, а не находит оптимум.
